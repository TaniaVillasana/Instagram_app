<!DOCTYPE html>
<html>
  <head>
    <title>InstagramApp</title>



    <%= stylesheet_link_tag    '/assets/application', media: 'all', 'data-turbolinks-track': 'reload' %>
   <%= javascript_include_tag '/assets/application', 'data-turbolinks-track': 'reload' %>





    <%= csrf_meta_tags %>
  </head>

  <body>
    <!-- Navigation -->
    <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
        <div class="container">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">

                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">

                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <%= link_to "Instagram", root_url, class: "navbar-brand" %>
            </div>
            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                    <li>
                      <% if user_signed_in? %>
                          <%= link_to "Subir Foto", new_post_path %>
                      <% end %>


                    </li>
                    <%- if current_user %>
                  <li>
                    <%= link_to "Cerrar Sesión", destroy_user_session_path, method: :delete, confirm: "Seguro que deseas salir?" %>
                    <!--<a href="<%= destroy_user_session_path %>" data-method='delete' >Salir</a>-->
                  </li>
                    <% else %>
                  <li>
                    <%= link_to "Iniciar sesion", new_user_session_path %>
                  </li>
                      <li>
                        <%= link_to "Registrarte", new_user_registration_path %>
                      </li>
                  <% end %>
                </ul>
            </div>
            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container -->
    </nav>

    <!-- Page Content -->
    <div class="container">

        <div class="row">
            <div class="col-lg-12 text-center">
              <% flash.each do |a, b| %>
                  <div class="alert alert-success"> <%= b %> </div>
                  <% end %>
                <%= yield %>
            </div>
        </div>
        <!-- /.row -->

    </div>
    <!-- /.container -->
    <style>body{padding-top:70px;}</style>




</body>

</html>
